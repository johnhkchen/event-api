# Event API Environment Variables
# Copy this file to .env and fill in your values

# Database Configuration (Development)
POSTGRES_DB=event_api_dev
POSTGRES_USER=event_api
POSTGRES_PASSWORD=development_password
DB_PORT=5432

# Database URL (will be auto-generated in development)
# DATABASE_URL=postgresql://event_api:development_password@localhost:5432/event_api_dev

# AI/LLM Services
OPENAI_API_KEY=sk-your-openai-key-here

# Service Configuration (Development)
NODE_ENV=development
MIX_ENV=dev
HONO_PORT=3000
ELIXIR_PORT=4000
BAML_PORT=8080

# Production-only variables (set via Coolify UI in production)
# IMAGE_TAG=main-abc123
# POSTGRES_PASSWORD=secure_production_password